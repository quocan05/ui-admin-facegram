export enum EUserDetailTabItem {
  DETAIL_PROFILE = 'detail-profile',
  AUTH_METHOD = 'auth-method',
  DIETARY_PROFILE = 'dietary-profile',
  ROLE = 'role',
  PERMISSION = 'permission',
  LOG = 'log',
}

export enum EDietDetailTabItem {
  INFO = 'info',
}

export enum EIngredientDetailTabItem {
  INFO = 'info',
}

export enum EVenueDetailTabItem {
  VENUE = 'venue',
  AUTH = 'auth',
  MENUS = 'menus',
  ACTIONS = 'actions',
  HOURS_REGULAR = 'hours-regular',
  HOURS_SPECIAL = 'hours-special',
}

export enum EVenueMenuDetailTabItem {
  INFORMATION = 'information',
  HOURS = 'hours',
  SPECIAL = 'special-hours',
  CUSTOMISATIONS = 'customisations',
}

export enum EVenueGroupDetailTabItem {
  VENUE_GROUP = 'venue-group',
  VENUES = 'venues',
  AUTH = 'auth',
  MENUS = 'menus',
  ACTIONS = 'actions',
}

export enum EMenuDetailTabItem {
  MENU = 'menu',
  SUMMARY = 'summary',
}

export enum EDietTabItem {
  DEFAULT = '',
}

export enum EDietCategoryTabItem {
  DEFAULT = '',
}

export enum EDietFootFlagTabItem {
  DEFAULT = '',
}

export enum EMenuTabItem {
  DEFAULT = 'menu',
  MENU_ITEMS = 'menu-items',
}

export enum EMenuItemTabItem {
  DEFAULT = 'menu-item',
  INGREDIENTS = 'menu-item-ingredients',
}

export enum EMenuItemIngredientTabItem {
  DEFAULT = '',
}
